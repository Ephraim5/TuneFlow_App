import React from 'react';
import { StyleSheet, View } from 'react-native';

const Pop = () => {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({})

export default Pop;
